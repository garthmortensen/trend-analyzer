# Database Configuration
# Override values from config/infrastructure.yml
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aca_health
DB_USERNAME=etl
DB_PASSWORD=etl
# Note: Schema is configured in config/infrastructure.yml (currently: dw)

# Database Performance Tuning
# Statement timeout in seconds (default: 30)
DB_STATEMENT_TIMEOUT_SECONDS=30

# OpenAI Configuration
# Required for AI-powered analysis features
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Performance Tuning (optional)
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_SECONDS=30

# Agent Configuration (optional)
# Maximum steps for single-shot agent (default: 1)
AGENT_MAX_STEPS=1
