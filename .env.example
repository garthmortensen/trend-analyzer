# Database Configuration
# Override values from config/infrastructure.yml
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aca_health
DB_USERNAME=etl
DB_PASSWORD=etl
# Note: Schema is configured in config/infrastructure.yml (currently: dw)

# Database Performance Tuning
# Statement timeout in seconds (default: 30)
DB_STATEMENT_TIMEOUT_SECONDS=30

# OpenAI Configuration
# Required for AI-powered analysis features
OPENAI_API_KEY=your-api-key-here

# Note: OpenAI model, timeout, and max_iterations are now configured in config/analysis.yml
# You can still override these with environment variables if needed:
# OPENAI_MODEL=gpt-4o-mini
# AGENT_MAX_ITERATIONS=10
